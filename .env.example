# 复制为 .env.local 并填入；.env.local 已被 .gitignore（*.local）忽略，
# 仅在构建时烘进 exe，不会进入公开仓库源码。
#
# 内置公益 DeepSeek Key：用户不填自己的 Key 时回退到此 Key。
VITE_DS_KEY=
# 可选：覆盖默认 DeepSeek 端点（一般留默认即可）。
VITE_DS_BASE_URL=https://api.deepseek.com/anthropic
